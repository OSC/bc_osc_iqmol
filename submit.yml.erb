<%-
  ppn = num_cores.blank? ? 28 : num_cores.to_i

  slurm_args = [ "--nodes", "1", "--ntasks-per-node", "#{ppn}" ]
%>
---
batch_connect:
  template: vnc
script:
  native:
  <%- slurm_args.each do |arg| %>
    - "<%= arg %>"
  <%- end %>

<%-
  slurm_args = [ "--nodes", "1", "--ntasks-per-node", "#{num_cores.to_i}" ]
%>
---
batch_connect:
  template: vnc
script:
  native:
  <%- slurm_args.each do |arg| %>
    - "<%= arg %>"
  <%- end %>
